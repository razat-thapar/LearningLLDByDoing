package com.lld.two.c_builder_pattern.g_production_ready_fix_builderPattern_mutable;

public class SalesClient {
    public static void main(String[] args) {
        // Scenario 1: Create student with university , name, gradYear, age.
        //validations:
        // age < 30
        // gradYear < currYear.
        // A sales client will have different perspective for a student.
        // he will need different set of attributes of a student compared to instructor.
        //e.g., sales client don't need psp as student is not enrolled yet. 
    }
}
